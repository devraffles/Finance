# Task 05 - Configuracoes Base

## Objetivo

Criar a infraestrutura base de aplicacao: Prisma, autenticacao, wrapper Claude, utilitarios e tipos de dominio.

## Arquivos

- `projeto/backend/src/lib/prisma.ts`
- `projeto/backend/src/lib/auth.ts`
- `projeto/backend/src/lib/claude.ts`
- `projeto/frontend/src/lib/utils.ts`
- `projeto/backend/src/types/financas.ts`

## `backend/src/lib/prisma.ts`

Implementar singleton do `PrismaClient` com hot reload seguro para Next.js.

## `backend/src/lib/auth.ts`

Implementar NextAuth com Credentials Provider:

- Validar email e senha com `bcrypt.compare`.
- Sessao JWT com `id`, `name` e `email`.
- Callbacks `session` e `jwt` para propagar `user.id`.
- Exportar `authOptions` e helper `getSession()` para Server Components.

## `backend/src/lib/claude.ts`

Exportar:

```ts
export async function callClaude(
  prompt: string,
  systemPrompt?: string,
): Promise<string>;

export async function categorizarTransacoes(
  transacoes: TransacaoBruta[],
): Promise<CategorizacaoResult[]>;

export async function gerarInsights(
  dados: DadosFinanceiros,
): Promise<Insight[]>;
```

Regras:

- Usar `claude-3-5-haiku-20241022` para categorizacao.
- Usar `claude-3-5-sonnet-20241022` para insights.
- Se `ANTHROPIC_API_KEY` estiver ausente, retornar estado controlado sem quebrar o app.
- Usar timeout de 30s com `AbortController`.

## `frontend/src/lib/utils.ts`

Exportar:

```ts
export function cn(...inputs: ClassValue[]): string;
export function formatCurrency(value: number): string;
export function formatDate(date: Date | string): string;
export function formatPercent(value: number, digits?: number): string;
export function calcularVariacao(atual: number, anterior: number): number;
export function gerarCorAleatoria(): string;
export function parseCSVNubank(csvText: string): TransacaoBruta[];
export function slugify(str: string): string;
```

## Tipos minimos

Incluir em `backend/src/types/financas.ts`:

- `ContaComSaldo`
- `TransacaoComConta`
- `InvestimentoComAportes`
- `DashboardResumo`
- `FluxoCaixaMensal`
- `Insight`
- `InsightTipo`
- `CategorizacaoResult`
- `DadosFinanceiros`
- `TransacaoBruta`
- `FiltrosTransacao`
- `PaginacaoParams`
- `PaginacaoResult<T>`

## Checklist

- [ ] Nenhum segredo e exposto no client.
- [ ] Nenhum `any` foi usado.
- [ ] Utilitarios formatam moeda em `pt-BR` com `R$`.
- [ ] Datas de exibicao usam `dd/MM/yyyy`.
- [ ] Claude falha de forma controlada sem API key.

# Task 02 - Fundacao do Projeto Next.js

## Objetivo

Criar a fundacao do workspace em `projeto/`, com Next.js 14 em `projeto/frontend/`, backend interno em `projeto/backend/`, pnpm, dependencias e Prisma com PostgreSQL.

## Comando de criacao

Execute a partir da raiz do workspace `projeto/`, criando o app em `frontend/`:

```bash
pnpm dlx create-next-app@latest frontend \
  --typescript \
  --tailwind \
  --eslint \
  --app \
  --src-dir \
  --import-alias "@/*" \
  --no-git
```

## Dependencias runtime

Instale dependencias de UI, charts, formularios e cliente auth no pacote `frontend`:

```bash
pnpm --filter frontend add \
  next-auth \
  react-hook-form @hookform/resolvers zod \
  recharts \
  lucide-react \
  dayjs \
  @radix-ui/react-dialog @radix-ui/react-dropdown-menu \
  @radix-ui/react-select @radix-ui/react-alert-dialog \
  @radix-ui/react-progress @radix-ui/react-tabs \
  @radix-ui/react-popover @radix-ui/react-tooltip \
  sonner \
  clsx tailwind-merge \
  class-variance-authority \
  react-dropzone
```

Instale dependencias server-side e de dominio no pacote `backend`:

```bash
pnpm --filter @financas360/backend add \
  prisma @prisma/client @auth/prisma-adapter next-auth \
  ai @ai-sdk/google \
  zod \
  dayjs \
  cpf-cnpj-validator \
  bcryptjs
```

## Dependencias dev

Instale ferramentas compartilhadas na raiz `projeto/` quando precisarem ficar disponiveis para scripts operacionais:

```bash
pnpm add -D -w @types/node ts-node tsx prettier eslint-config-prettier
```

Instale tipos e TypeScript nos pacotes quando o pacote precisar validar isoladamente:

```bash
pnpm --filter frontend add -D @types/node @types/react @types/react-dom typescript
pnpm --filter @financas360/backend add -D @types/node @types/bcryptjs typescript
```

## Prisma

Inicialize ou mantenha o Prisma em `projeto/backend/prisma/`. Se usar `prisma init`, mova o schema gerado para o backend e preserve o datasource `postgresql`.

```bash
pnpm exec prisma init --datasource-provider postgresql --schema backend/prisma/schema.prisma
```

## Scripts minimos

Criar scripts:

- `dev`
- `build`
- `start`
- `lint`
- `format`
- `format:check`
- `typecheck`

### Testes obrigatorios para esta task

- [ ] Escrever testes unitarios para toda logica de negocio nova (Vitest)
- [ ] Escrever testes de integracao para os endpoints ou server actions criados (Supertest / Vitest)
- [ ] Escrever testes de componente para todo componente novo (Testing Library)
- [ ] Escrever ao menos 1 teste E2E para o fluxo principal da funcionalidade (Playwright)
- [ ] Garantir que `pnpm test` passa sem erros antes de marcar a task como concluida
## Checklist
- [ ] Projeto criado dentro de `projeto/`.
- [ ] Frontend criado em `projeto/frontend/`.
- [ ] Backend interno criado em `projeto/backend/`.
- [ ] `pnpm-lock.yaml` existe.
- [ ] Nao existe `package-lock.json` nem `yarn.lock`.
- [ ] Prisma inicializado em `projeto/backend/prisma/schema.prisma` com provider `postgresql`.
- [ ] Prettier e ESLint configurados.
- [ ] shadcn/ui pode ser usado como base de componentes reutilizaveis.
- [ ] Day.js e biblioteca de validacao CPF/CNPJ estao disponiveis quando a implementacao chegar aos formularios e APIs correspondentes.
- [ ] `pnpm run lint`, `pnpm run format:check` e `pnpm run typecheck` existem.
- [ ] Testes unitarios escritos e passando
- [ ] Testes de integracao escritos e passando
- [ ] Testes de componente escritos e passando (se houver UI)
- [ ] Ao menos 1 teste E2E cobrindo o fluxo principal
- [ ] `pnpm test` executado com sucesso
- [ ] Cobertura de codigo nao regrediu
